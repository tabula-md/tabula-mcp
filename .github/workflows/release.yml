name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout release tag
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Setup Java for Firebase emulators
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: Use current npm with trusted publishing support
        run: npm install --global npm@latest

      - name: Install dependencies
        run: npm ci

      - name: Load the checked-in release manifest
        id: manifest
        run: node scripts/export-release-manifest.mjs

      - name: Verify production deployment credentials
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: node scripts/check-release-secrets.mjs

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Verify tag and release metadata
        id: release
        run: |
          node scripts/check-release-contract.mjs --tag "$GITHUB_REF_NAME"
          echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Run the release gate and build artifacts
        run: npm run release:verify

      - name: Checkout Tabula browser dependencies
        uses: actions/checkout@v6
        with:
          repository: ${{ steps.manifest.outputs.tabula_md_repository }}
          ref: ${{ steps.manifest.outputs.tabula_md_ref }}
          path: deps/tabula-md

      - name: Checkout Room service
        uses: actions/checkout@v6
        with:
          repository: ${{ steps.manifest.outputs.tabula_room_repository }}
          ref: ${{ steps.manifest.outputs.tabula_room_ref }}
          path: deps/tabula-room

      - name: Checkout Copy service
        uses: actions/checkout@v6
        with:
          repository: ${{ steps.manifest.outputs.tabula_json_repository }}
          ref: ${{ steps.manifest.outputs.tabula_json_ref }}
          path: deps/tabula-json

      - name: Verify pinned interoperability commits
        run: node scripts/check-interoperability-checkouts.mjs

      - name: Install interoperability dependencies
        run: npm ci --prefix deps/tabula-md && npm ci --prefix deps/tabula-room && npm ci --prefix deps/tabula-json

      - name: Verify live collaboration and Copy browser round trips
        env:
          TABULA_MD_REPO_DIR: ${{ github.workspace }}/deps/tabula-md
          TABULA_ROOM_REPO_DIR: ${{ github.workspace }}/deps/tabula-room
          TABULA_JSON_REPO_DIR: ${{ github.workspace }}/deps/tabula-json
        run: npm run test:e2e:local-collab

      - name: Resolve release provenance
        run: npm run build:release-manifest -- --tag "$GITHUB_REF_NAME" --commit "$GITHUB_SHA"

      - name: Publish npm package through OIDC
        run: npm publish --access public --ignore-scripts

      - name: Create GitHub Release and upload MCPB artifacts
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.release.outputs.version }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            "dist/tabula-mcp-${VERSION}.mcpb#Claude Desktop extension ${VERSION}" \
            "dist/tabula-mcp-${VERSION}.mcpb.sha256#SHA-256 checksum ${VERSION}" \
            "dist/tabula-mcp.mcpb#Latest Claude Desktop extension" \
            "dist/tabula-mcp.mcpb.sha256#Latest SHA-256 checksum" \
            "dist/release-manifest.json#Resolved release manifest" \
            --title "Tabula MCP ${VERSION}" \
            --generate-notes \
            --verify-tag

      - name: Deploy the production Cloudflare Worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npm run deploy:cloudflare

      - name: Verify npm, GitHub, and production surfaces
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: node scripts/verify-published-release.mjs
